services:
  backend:
    build:
      context: ..
      dockerfile: docker/backend.Dockerfile
    environment:
      COSMOS_CORS_ORIGINS: http://127.0.0.1:5173
    ports:
      - "8000:8000"
    volumes:
      - cosmos-runtime:/var/lib/cosmos

volumes:
  cosmos-runtime:
